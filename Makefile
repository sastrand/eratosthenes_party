#----------------------------------------------------------------------
# Makefile for CS415/515 Assignment 2
#----------------------------------------------------------------------

CC = gcc

%-omp: %-omp.c
	$(CC) -fopenmp -o $@ $< -g -lm

%-pthd%: %-pthd%.c
	gcc -pthread -o $@ $< -g -lm

